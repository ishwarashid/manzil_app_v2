import 'package:flutter/material.dart';

class PassengerTrackingMap extends StatelessWidget {
  const PassengerTrackingMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
